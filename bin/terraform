#!/bin/bash

export TF_VAR_project=$GCP_PROJECT_NAME

BACKEND=$PWD/terraform/backend.conf

#TODO: create keyfile.json file from env variable

: > "$BACKEND"
{
  echo "bucket      = \"$GCP_BUCKET_NAME\"";
  echo 'credentials = "keyfile.json"';
} >> "$BACKEND"

terraform -chdir=terraform "$@"
